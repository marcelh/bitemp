   
mongo {
    host = "localhost"
    port = 27017
    database = "test"
    collections {
        bitemp = "bitemp_data"
    }
}

statistics {
    enabled = true
    directory = ${HOME}"/bitemp/statistics"
}